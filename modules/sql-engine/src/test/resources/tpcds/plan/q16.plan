Sort
    collation: [order count ASC]
    fetch: 100
    est: (rows=100)
  Project
      projection: [order count, CAST(total shipping cost):DECIMAL(14, 2), CAST(total net profit):DECIMAL(14, 2)]
      est: (rows=981399350335625)
    ColocatedSortAggregate
        fieldNames: [order count, total shipping cost, total net profit]
        collation: []
        group: []
        aggregation: [COUNT(CS_ORDER_NUMBER), SUM(total shipping cost), SUM(total net profit)]
        est: (rows=981399350335625)
      ColocatedSortAggregate
          fieldNames: [CS_ORDER_NUMBER, total shipping cost, total net profit]
          collation: [CS_ORDER_NUMBER ASC]
          group: [CS_ORDER_NUMBER]
          aggregation: [SUM(CS_EXT_SHIP_COST), SUM(CS_NET_PROFIT)]
          est: (rows=981399350335625)
        Project
            fieldNames: [CS_ORDER_NUMBER, CS_EXT_SHIP_COST, CS_NET_PROFIT]
            projection: [CS_ORDER_NUMBER, CS_EXT_SHIP_COST, CS_NET_PROFIT]
            est: (rows=4906996751678128)
          Filter
              predicate: IS NULL($f1)
              est: (rows=4906996751678128)
            MergeJoin
                predicate: =(CS_ORDER_NUMBER, CR_ORDER_NUMBER)
                type: left
                est: (rows=19627987006712512)
              HashJoin
                  predicate: AND(=(CS_WAREHOUSE_SK, CS_WAREHOUSE_SK0), =(CS_ORDER_NUMBER, CS_ORDER_NUMBER0))
                  type: inner
                  est: (rows=4540509525340)
                HashJoin
                    predicate: =(CS_CALL_CENTER_SK, CC_CALL_CENTER_SK)
                    type: inner
                    est: (rows=120009)
                  HashJoin
                      predicate: =(CS_SHIP_ADDR_SK, CA_ADDRESS_SK)
                      type: inner
                      est: (rows=120009)
                    HashJoin
                        predicate: =(CS_SHIP_DATE_SK, D_DATE_SK)
                        type: inner
                        est: (rows=360387)
                      Exchange
                          distribution: single
                          est: (rows=1441548)
                        Sort
                            collation: [CS_ORDER_NUMBER ASC, CS_WAREHOUSE_SK ASC]
                            est: (rows=1441548)
                          TableScan
                              table: PUBLIC.CATALOG_SALES
                              fieldNames: [CS_SHIP_DATE_SK, CS_SHIP_ADDR_SK, CS_CALL_CENTER_SK, CS_WAREHOUSE_SK, CS_ORDER_NUMBER, CS_EXT_SHIP_COST, CS_NET_PROFIT]
                              est: (rows=1441548)
                      Exchange
                          distribution: single
                          est: (rows=18262)
                        TableScan
                            table: PUBLIC.DATE_DIM
                            predicate: SEARCH(D_DATE, Sarg[[1999-04-01..1999-05-31]])
                            fieldNames: [D_DATE_SK, D_DATE]
                            est: (rows=18262)
                    Exchange
                        distribution: single
                        est: (rows=16650)
                      TableScan
                          table: PUBLIC.CUSTOMER_ADDRESS
                          predicate: =(CA_STATE, _UTF-8'IL')
                          fieldNames: [CA_ADDRESS_SK, CA_STATE]
                          est: (rows=16650)
                  Exchange
                      distribution: single
                      est: (rows=6)
                    TableScan
                        table: PUBLIC.CALL_CENTER
                        predicate: SEARCH(CC_COUNTY, Sarg[_UTF-8'Bronx County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Maverick County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Mesa County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Raleigh County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Richland County':VARCHAR(30) CHARACTER SET "UTF-8"]:VARCHAR(30) CHARACTER SET "UTF-8")
                        fieldNames: [CC_CALL_CENTER_SK, CC_COUNTY]
                        est: (rows=6)
                ColocatedHashAggregate
                    group: [CS_WAREHOUSE_SK0, CS_ORDER_NUMBER0]
                    aggregation: []
                    est: (rows=1681545886)
                  Project
                      fieldNames: [CS_WAREHOUSE_SK0, CS_ORDER_NUMBER0]
                      projection: [CS_WAREHOUSE_SK$0, CS_ORDER_NUMBER$0]
                      est: (rows=4670960795)
                    HashJoin
                        predicate: AND(=(CS_ORDER_NUMBER$0, CS_ORDER_NUMBER), <>(CS_WAREHOUSE_SK$0, CS_WAREHOUSE_SK))
                        fieldNames: [CS_WAREHOUSE_SK, CS_ORDER_NUMBER, CS_WAREHOUSE_SK$0, CS_ORDER_NUMBER$0]
                        type: inner
                        est: (rows=4670960795)
                      Exchange
                          distribution: single
                          est: (rows=1441548)
                        TableScan
                            table: PUBLIC.CATALOG_SALES
                            fieldNames: [CS_WAREHOUSE_SK, CS_ORDER_NUMBER]
                            est: (rows=1441548)
                      ColocatedHashAggregate
                          group: [CS_WAREHOUSE_SK, CS_ORDER_NUMBER]
                          aggregation: []
                          est: (rows=43203)
                        Project
                            fieldNames: [CS_WAREHOUSE_SK, CS_ORDER_NUMBER]
                            projection: [CS_WAREHOUSE_SK, CS_ORDER_NUMBER]
                            est: (rows=120009)
                          HashJoin
                              predicate: =(CS_CALL_CENTER_SK, CC_CALL_CENTER_SK)
                              type: inner
                              est: (rows=120009)
                            HashJoin
                                predicate: =(CS_SHIP_ADDR_SK, CA_ADDRESS_SK)
                                type: inner
                                est: (rows=120009)
                              HashJoin
                                  predicate: =(CS_SHIP_DATE_SK, D_DATE_SK)
                                  type: inner
                                  est: (rows=360387)
                                Exchange
                                    distribution: single
                                    est: (rows=1441548)
                                  TableScan
                                      table: PUBLIC.CATALOG_SALES
                                      fieldNames: [CS_SHIP_DATE_SK, CS_SHIP_ADDR_SK, CS_CALL_CENTER_SK, CS_WAREHOUSE_SK, CS_ORDER_NUMBER]
                                      est: (rows=1441548)
                                Exchange
                                    distribution: single
                                    est: (rows=18262)
                                  TableScan
                                      table: PUBLIC.DATE_DIM
                                      predicate: SEARCH(D_DATE, Sarg[[1999-04-01..1999-05-31]])
                                      fieldNames: [D_DATE_SK, D_DATE]
                                      est: (rows=18262)
                              Exchange
                                  distribution: single
                                  est: (rows=16650)
                                TableScan
                                    table: PUBLIC.CUSTOMER_ADDRESS
                                    predicate: =(CA_STATE, _UTF-8'IL')
                                    fieldNames: [CA_ADDRESS_SK, CA_STATE]
                                    est: (rows=16650)
                            Exchange
                                distribution: single
                                est: (rows=6)
                              TableScan
                                  table: PUBLIC.CALL_CENTER
                                  predicate: SEARCH(CC_COUNTY, Sarg[_UTF-8'Bronx County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Maverick County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Mesa County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Raleigh County':VARCHAR(30) CHARACTER SET "UTF-8", _UTF-8'Richland County':VARCHAR(30) CHARACTER SET "UTF-8"]:VARCHAR(30) CHARACTER SET "UTF-8")
                                  fieldNames: [CC_CALL_CENTER_SK, CC_COUNTY]
                                  est: (rows=6)
              Project
                  fieldNames: [CR_ORDER_NUMBER, $f1]
                  projection: [CR_ORDER_NUMBER, true]
                  est: (rows=28813)
                Sort
                    collation: [CR_ORDER_NUMBER ASC]
                    est: (rows=28813)
                  ReduceHashAggregate
                      fieldNames: [CR_ORDER_NUMBER]
                      group: [CR_ORDER_NUMBER]
                      aggregation: []
                      est: (rows=28813)
                    Exchange
                        distribution: single
                        est: (rows=28813)
                      MapHashAggregate
                          fieldNames: [CR_ORDER_NUMBER, _GROUP_ID]
                          group: [CR_ORDER_NUMBER]
                          aggregation: []
                          est: (rows=28813)
                        TableScan
                            table: PUBLIC.CATALOG_RETURNS
                            fieldNames: [CR_ORDER_NUMBER]
                            est: (rows=144067)
