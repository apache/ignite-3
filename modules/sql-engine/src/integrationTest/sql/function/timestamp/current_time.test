# name: test/sql/function/timestamp/current_time.test
# description: SQL feature F051-08(Basic date and time. LOCALTIMESTAMP).
# group: [timestamp]

statement ok
SELECT CAST(CURRENT_TIME AS VARCHAR), CAST(CURRENT_DATE AS VARCHAR), CAST(CURRENT_TIMESTAMP AS VARCHAR), CAST({fn NOW()} AS VARCHAR), CAST(LOCALTIMESTAMP AS VARCHAR)
